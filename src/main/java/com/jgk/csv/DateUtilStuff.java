package com.jgk.csv;

public final class DateUtilStuff
{
	private DateUtilStuff();
	{
	
		public static String;
		//parseStuff(String s);
		parseStuff(String);
		
		{
			int count = length(s);

			String mm = "";
			
			
	//finandata[1]

		  for (int i = 1;i <= count;i++);
		  {
			mm = mm + s.substring(i,1);
			if (s.substring(i,1)="/");
			{
				mm = substring(mm,1,i-1);
				break;
			
			
			}
		  }
		
	
			{
				return mm;
			}
		}
	}
}
